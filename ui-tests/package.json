{
  "name": "r-jupytergis-ui-tests",
  "version": "0.0.1",
  "description": "JupyterLab r-jupytergis Integration Tests",
  "private": true,
  "scripts": {
    "start": "jupyter lab --config jupyter_server_test_config.py --port 53729",
    "test": "npx --no playwright test",
    "test:update": "npx --no playwright test --update-snapshots"
  },
  "devDependencies": {
    "@jupyterlab/galata": "^5.6.0",
    "@playwright/test": "^1.61.0"
  }
}
